name: 'C# Test'

on: push

jobs:
  Test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET Core 5
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.202
       
    - name: Is F# compiler here
      run: fsc AAAAAAAA
